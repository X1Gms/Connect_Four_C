#ifndef MENU_H
#define MENU_H

#include "types.h"

// Inicialização das funções do ficheiro menu.c

void showMenu();
void executeOption();
void resetBoard(char board[ROWS][COLS]);
void printBoard(char board[ROWS][COLS]);

#endif
